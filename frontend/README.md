# AssetFlow frontend

The Next/Vinext routes under `app/` are the frontend application. The booking and maintenance screens consume the backend service contracts and keep workflow rules out of React components.
