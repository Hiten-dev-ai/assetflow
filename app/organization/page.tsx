import { FeatureShell } from "../../components/FeatureShell";

export default function OrganizationPage() {
  return (
    <FeatureShell title="Organization setup">
      <section className="feature-panel">
        <p className="eyebrow accent">Workspace structure</p>
        <h2>Organization setup</h2>
        <p>Manage departments, locations, asset categories, and access roles from this workspace.</p>
      </section>
    </FeatureShell>
  );
}
